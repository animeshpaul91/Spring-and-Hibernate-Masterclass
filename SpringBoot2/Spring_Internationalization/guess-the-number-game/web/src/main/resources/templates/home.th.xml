<xml lang="en" xmlns:th="http://www.thymeleaf.org">
    <thlogic>
        <attr sel="#headId"
              th:replace="~{fragments/head :: headFragment}"/>
        <attr sel="#anchor-tag"
              th:href="@{__${T(academy.learnprogramming.util.GameMappings).PLAY}__}" th:text="Play"/>
        <attr sel="#footerId"
              th:replace="~{fragments/footer::footerFragment}"/>
    </thlogic>
</xml>
