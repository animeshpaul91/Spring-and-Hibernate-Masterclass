# configure server port
# server.port=8585

# configure my props
coach.name=Mickey Mouse

# Customize info endpoint with custom properties
info.app.name=My Super Cool App
info.app.description=A crazy fun app, yohoo!
info.app.version=1.0.0
coach.name=Mickey Mouse
team.name=The Mouse Club

# Expose other endpoints
management.endpoints.web.exposure.include=*

# Exclude individual endpoints with comma separated list. They will not be available
# management.endpoints.web.exposure.exclude=health,info

# Base path for Actuator Endpoints
management.endpoints.web.base-path=/actuator

# Default user name for Spring Security
spring.security.user.name=admin

# Default user name for Spring Security
spring.security.user.password=test123